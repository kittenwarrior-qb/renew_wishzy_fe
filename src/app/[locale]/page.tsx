import { CategoryList } from '@/shared/components/category/category-list';

const page = () => {
  return (
    <div style={{ padding: '2rem' }}>
      <CategoryList />
    </div>
  );
};

export default page;
