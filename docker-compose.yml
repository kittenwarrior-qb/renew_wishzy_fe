version: '3.8'

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    networks:
      - wishzy-network
    restart: unless-stopped

networks:
  wishzy-network:
    driver: bridge